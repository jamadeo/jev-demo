[package]
name = "jev-demo"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
goose-providers = { path = "../goose/crates/goose-providers", features = ["rustls-tls"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
